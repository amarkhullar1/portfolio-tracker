services:
  portfolio:
    build: .
    container_name: portfolio-dashboard
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - DATA_DIR=/app/data
